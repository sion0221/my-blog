---
import Layout from "@/layouts/Layout.astro";
import { supabase } from "@/lib/supabase";
import type { Post } from "@/types/blog";

const { data: posts } = await supabase
  .from("posts")
  .select("*")
  .order("created_at", { ascending: false });
---

<Layout title="SION.LOG">
  <div class="space-y-20">
    <section class="py-16 md:py-24">
      <h1
        class="text-5xl md:text-7xl font-extrabold tracking-tight text-white mb-8 leading-[1.1]"
      >
        Learning, Coding<br />
        <span
          class="bg-linear-to-r from-blue-400 to-emerald-400 bg-clip-text text-transparent"
        >
          & writing
        </span>
      </h1>
      <p
        class="text-lg md:text-xl text-slate-400 max-w-2xl leading-relaxed font-medium"
      >
        어제보다 더 나은 프론트엔드 개발자로 나아가기 위해서,<span
          class="text-slate-200"
        >
          배움을 통해 얻은 경험</span
        >을 기록으로 남기고 있습니다.
      </p>

      <div class="mt-10 flex gap-4">
        <div
          class="px-4 py-1.5 rounded-full border border-slate-700 bg-slate-800/50 text-xs font-semibold text-blue-300"
        >
          #Frontend
        </div>
        <div
          class="px-4 py-1.5 rounded-full border border-slate-700 bg-slate-800/50 text-xs font-semibold text-emerald-300"
        >
          #Study
        </div>
      </div>
    </section>
    <div class="grid gap-8 md:grid-cols-2">
      {
        posts?.map((post: Post) => (
          <article class="group p-8 rounded-3xl bg-slate-900/50 border border-slate-800 hover:border-slate-700 hover:bg-slate-900 transition-all duration-300">
            <div class="flex flex-col h-full">
              <span class="text-xs font-bold tracking-widest text-blue-400 uppercase mb-4">
                Post
              </span>
              <h2 class="text-2xl font-bold text-white mb-4 group-hover:text-blue-300 transition">
                <a href={`/posts/${post.id}`}>{post.title}</a>
              </h2>
              <p class="text-slate-400 line-clamp-2 mb-8 grow leading-relaxed">
                {post.content}
              </p>
              <div class="flex items-center justify-between text-xs text-slate-500 font-medium">
                <span>
                  {post.created_at
                    ? new Date(post.created_at).toLocaleDateString()
                    : ""}
                </span>{" "}
                <span class="px-3 py-1 rounded-full bg-slate-800 text-slate-300">
                  Views {post.views}
                </span>
              </div>
            </div>
          </article>
        ))
      }
    </div>
  </div>
</Layout>
